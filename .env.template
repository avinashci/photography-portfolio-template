# ========================================
# PHOTOGRAPHY PORTFOLIO TEMPLATE - ENVIRONMENT CONFIGURATION
# ========================================
#
# 🎯 TEMPLATE USERS: Copy this file to .env.local and update with your information
#
# Required for basic setup:
# - MONGODB_URI (database connection)
# - PAYLOAD_SECRET (CMS security - generate a secure 32+ character string)
# - NEXT_PUBLIC_SERVER_URL (your domain for production)
# - REVALIDATE_SECRET (auto-revalidation when content changes)
#

# =============================================================================
# 🏠 SITE CONFIGURATION (Required)
# =============================================================================

# Your domain (Update with your actual domain for production)
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"

# =============================================================================
# 🛢️ DATABASE & CMS (Required)
# =============================================================================

# MongoDB Connection (Required - Update database name)
MONGODB_URI="mongodb://localhost:27017/your-portfolio-db"

# Payload CMS Security (Required - Generate a secure 32+ character string)
# Use: openssl rand -base64 32
PAYLOAD_SECRET="your-super-secret-key-32-characters-minimum-length-required"

# Auto-revalidation when CMS content changes (Required)
# Use: openssl rand -base64 32
REVALIDATE_SECRET="your-revalidation-secret-key"

# =============================================================================
# 🏗️ ENVIRONMENT
# =============================================================================

# Node Environment
NODE_ENV="development"